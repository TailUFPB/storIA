{{- if .Values.autoscaling.api.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "meu-chart.fullname" . }}-api-hpa  # Nome único por release
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    name: {{ include "meu-chart.fullname" . }}-api  # Referência correta
    apiVersion: apps/v1
    kind: Deployment
  minReplicas: {{ .Values.autoscaling.api.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.api.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.autoscaling.api.targetCPU }}
{{- end }}